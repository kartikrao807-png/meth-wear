"use client";

import { motion } from "framer-motion";
import { useState, useEffect } from "react";

const Button = ({ children, className = "", ...props }) => (
  <motion.button
    whileHover={{ scale: 1.05 }}
    whileTap={{ scale: 0.95 }}
    className={`px-4 py-2 rounded-full bg-white text-black font-semibold ${className}`}
    {...props}
  >
    {children}
  </motion.button>
);

const fadeUp = {
  hidden: { opacity: 0, y: 40 },
  show: {
    opacity: 1,
    y: 0,
    transition: { duration: 0.6 },
  },
};

const stagger = {
  hidden: {},
  show: {
    transition: {
      staggerChildren: 0.12,
    },
  },
};

export default function MethWears() {
  const [page, setPage] = useState("intro");
  const [cart, setCart] = useState([]);
  const [selected, setSelected] = useState(null);

  useEffect(() => {
    const t = setTimeout(() => setPage("home"), 1500);
    return () => clearTimeout(t);
  }, []);

  const products = [
    {
      id: 1,
      name: "Cyber Hoodie",
      price: 2499,
      img: "https://images.unsplash.com/photo-1521572163474-6864f9cf17ab?q=80&w=1200",
    },
    {
      id: 2,
      name: "Neon Jacket",
      price: 3999,
      img: "https://images.unsplash.com/photo-1523381210434-271e8be1f52b?q=80&w=1200",
    },
    {
      id: 3,
      name: "Future Tee",
      price: 1499,
      img: "https://images.unsplash.com/photo-1503342217505-b0a15ec3261c?q=80&w=1200",
    },
    {
      id: 4,
      name: "Dark Pants",
      price: 1999,
      img: "https://images.unsplash.com/photo-1514996937319-344454492b37?q=80&w=1200",
    },
  ];

  const addToCart = (item) => {
    setCart((prev) => [...prev, item]);
  };

  const total = cart.reduce((sum, item) => sum + item.price, 0);

  const BackgroundFX = () => (
    <>
      <motion.div
        animate={{ x: [0, 100, 0], y: [0, -50, 0] }}
        transition={{ duration: 12, repeat: Infinity }}
        className="absolute top-0 left-0 w-[500px] h-[500px] bg-cyan-500/20 blur-3xl rounded-full"
      />

      <motion.div
        animate={{ x: [0, -120, 0], y: [0, 80, 0] }}
        transition={{ duration: 16, repeat: Infinity }}
        className="absolute bottom-0 right-0 w-[400px] h-[400px] bg-blue-500/20 blur-3xl rounded-full"
      />

      <div className="absolute inset-0 bg-black/40" />
    </>
  );

  const Navbar = () => (
    <div className="flex justify-between items-center mb-8 relative z-20">
      <h1 className="text-2xl font-black tracking-[0.3em]">METH</h1>

      <div className="flex gap-6 text-sm">
        <button onClick={() => setPage("home")}>HOME</button>
        <button onClick={() => setPage("shop")}>SHOP</button>
        <button onClick={() => setPage("cart")}>
          BAG ({cart.length})
        </button>
      </div>
    </div>
  );

  // INTRO
  if (page === "intro") {
    return (
      <div className="h-screen bg-black flex items-center justify-center text-white overflow-hidden">
        <motion.h1
          initial={{ scale: 0 }}
          animate={{ scale: 1.2 }}
          transition={{ duration: 1 }}
          className="text-6xl font-black tracking-[0.4em]"
        >
          METH
        </motion.h1>
      </div>
    );
  }

  // HOME
  if (page === "home") {
    return (
      <div className="min-h-screen bg-black text-white overflow-hidden relative">
        <BackgroundFX />

        {/* BACKGROUND VIDEO */}
        <video
          autoPlay
          loop
          muted
          playsInline
          className="absolute inset-0 w-full h-full object-cover opacity-20"
          src="https://cdn.coverr.co/videos/coverr-chemical-reaction-in-lab-5171/1080p.mp4"
        />

        <div className="relative z-10 p-6">
          <Navbar />

          <motion.div
            variants={stagger}
            initial="hidden"
            animate="show"
            className="h-[80vh] flex flex-col justify-center"
          >
            <motion.h1
              variants={fadeUp}
              className="text-6xl md:text-8xl font-black leading-none"
            >
              JUST DROP
              <br />
              THE FUTURE
            </motion.h1>

            <motion.p
              variants={fadeUp}
              className="text-gray-300 mt-5 text-lg"
            >
              Chemistry × Streetwear
            </motion.p>

            <motion.div variants={fadeUp}>
              <Button
                className="mt-8"
                onClick={() => setPage("shop")}
              >
                Explore Collection
              </Button>
            </motion.div>
          </motion.div>
        </div>
      </div>
    );
  }

  // SHOP
  if (page === "shop") {
    return (
      <div className="min-h-screen bg-black text-white relative overflow-hidden">
        <BackgroundFX />

        <div className="relative z-10 p-6">
          <Navbar />

          <motion.div
            variants={stagger}
            initial="hidden"
            animate="show"
            className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6"
          >
            {products.map((item) => (
              <motion.div
                key={item.id}
                variants={fadeUp}
                whileHover={{ y: -10 }}
                className="bg-white/5 backdrop-blur-xl border border-white/10 rounded-3xl overflow-hidden group relative"
              >
                {/* CHEMISTRY HOVER */}
                <div className="absolute inset-0 opacity-0 group-hover:opacity-100 transition duration-500 pointer-events-none">
                  <svg
                    viewBox="0 0 200 200"
                    className="w-full h-full"
                  >
                    <line
                      x1="50"
                      y1="50"
                      x2="150"
                      y2="50"
                      stroke="cyan"
                      strokeWidth="2"
                      className="animate-pulse"
                    />
                    <line
                      x1="50"
                      y1="50"
                      x2="100"
                      y2="140"
                      stroke="cyan"
                      strokeWidth="2"
                      className="animate-pulse"
                    />
                    <line
                      x1="150"
                      y1="50"
                      x2="100"
                      y2="140"
                      stroke="cyan"
                      strokeWidth="2"
                      className="animate-pulse"
                    />

                    <circle cx="50" cy="50" r="6" fill="cyan" />
                    <circle cx="150" cy="50" r="6" fill="cyan" />
                    <circle cx="100" cy="140" r="6" fill="cyan" />
                  </svg>
                </div>

                <div onClick={() => setSelected(item)}>
                  <motion.img
                    whileHover={{ scale: 1.05 }}
                    transition={{ duration: 0.4 }}
                    src={item.img}
                    alt={item.name}
                    className="h-72 w-full object-cover"
                  />
                </div>

                <div className="p-4">
                  <h3 className="text-xl font-bold">{item.name}</h3>

                  <p className="text-cyan-300 mt-1">
                    ₹{item.price}
                  </p>

                  <Button
                    className="mt-4 w-full"
                    onClick={() => addToCart(item)}
                  >
                    Add To Cart
                  </Button>
                </div>
              </motion.div>
            ))}
          </motion.div>
        </div>

        {/* PRODUCT MODAL */}
        {selected && (
          <div className="fixed inset-0 bg-black/80 z-50 flex items-center justify-center p-6">
            <motion.div
              initial={{ scale: 0.8, opacity: 0 }}
              animate={{ scale: 1, opacity: 1 }}
              className="bg-neutral-900 rounded-3xl overflow-hidden max-w-md w-full"
            >
              <img
                src={selected.img}
                alt={selected.name}
                className="w-full h-96 object-cover"
              />

              <div className="p-6">
                <h2 className="text-3xl font-black">
                  {selected.name}
                </h2>

                <p className="text-cyan-300 mt-2 text-xl">
                  ₹{selected.price}
                </p>

                <Button
                  className="mt-6 w-full"
                  onClick={() => addToCart(selected)}
                >
                  Add To Cart
                </Button>

                <Button
                  className="mt-3 w-full"
                  onClick={() => setSelected(null)}
                >
                  Close
                </Button>
              </div>
            </motion.div>
          </div>
        )}
      </div>
    );
  }

  // CART
  if (page === "cart") {
    return (
      <div className="min-h-screen bg-black text-white relative overflow-hidden">
        <BackgroundFX />

        <div className="relative z-10 p-6">
          <Navbar />

          <h1 className="text-5xl font-black mb-10">YOUR BAG</h1>

          {cart.length === 0 ? (
            <p className="text-gray-400">Your bag is empty.</p>
          ) : (
            <>
              <div className="space-y-4">
                {cart.map((item, index) => (
                  <motion.div
                    key={index}
                    initial={{ opacity: 0, x: -20 }}
                    animate={{ opacity: 1, x: 0 }}
                    className="flex justify-between bg-white/5 border border-white/10 rounded-2xl p-4"
                  >
                    <span>{item.name}</span>
                    <span>₹{item.price}</span>
                  </motion.div>
                ))}
              </div>

              <div className="mt-10">
                <h2 className="text-3xl font-black">
                  Total: ₹{total}
                </h2>

                <Button
                  className="mt-6"
                  onClick={() => setPage("payment")}
                >
                  Checkout
                </Button>
              </div>
            </>
          )}
        </div>
      </div>
    );
  }

  // PAYMENT
  if (page === "payment") {
    return (
      <div className="h-screen bg-black text-white flex flex-col items-center justify-center">
        <h1 className="text-5xl font-black">PAYMENT</h1>

        <p className="mt-4 text-gray-400">
          Demo Checkout — ₹{total}
        </p>

        <Button
          className="mt-8"
          onClick={() => setPage("success")}
        >
          Pay Now
        </Button>
      </div>
    );
  }

  // SUCCESS
  if (page === "success") {
    return (
      <div className="h-screen bg-black text-white flex items-center justify-center">
        <motion.h1
          initial={{ scale: 0 }}
          animate={{ scale: 1 }}
          className="text-6xl font-black"
        >
          ORDER PLACED ✔
        </motion.h1>
      </div>
    );
  }

  return null;
}
